
// Rough upper bound (minutes) shown to users for how long a large document may take to render
// in-browser. Compilation happens locally via Typst, so there is no hard backend timeout.
export const MAX_MINUTES = 10
