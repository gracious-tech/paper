# typst-utils

Zero-dependency helpers for working with Typst markup. So far just provides `escape_typst` (for general Typst markup) and `escape_typst_str` (for "quoted string contents").

Works in browser and Node.

`npm install typst-utils`
